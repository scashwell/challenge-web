export function BibleProjectLogo() {
    const logoStyle = {
        color: 'var(--white)',
        fontSize: '1.5rem',
        fontWeight: 700,
    };

    return (
        <span style={logoStyle}>BibleProject</span>
    );
}
